{
  "name": "kord",
  "version": "1.0.0",
  "description": "Light discord app",
  "main": "index.js",
  "author": "",
  "scripts": {
    "start": "electron .",
    "build:app": "rm appdir/ -rf && cd appsrc && npm install && npm run build:kord && cp -r kord ../appdir",
    "dev:app" :"rm appdir/ -rf && cd appsrc && npm install && npm run dev:kord && cp -r kord ../appdir",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:all": "electron-builder -wl",
    "build:win": "electron-builder --win",
    "build:linux": "electron-builder --linux"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Samenal-sys/kord.git"
  },
  "keywords": [],
  "license": "AGPL-3.0-only",
  "type": "commonjs",
  "bugs": {
    "url": "https://github.com/Samenal-sys/kord/issues"
  },
  "homepage": "https://github.com/Samenal-sys/kord#readme",
  "build": {
    "appId": "com.samenalsys.kord",
    "productName": "kord",
    "files": [
      "**/*"
    ],
    "win": {
      "target": [
        "zip"
      ]
    },
    "linux": {
      "target": [
        "AppImage",
        "deb",
        "rpm"
      ]
    }
  },
  "devDependencies": {
    "electron": "^30.0.0",
    "electron-builder": "^24.13.3"
  },
  "allowScripts": {
    "electron@30.5.1": true
  },
  "dependencies": {
    "serve-handler": "^6.1.7"
  }
}
